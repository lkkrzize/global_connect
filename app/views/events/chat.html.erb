<h4 style="padding: 20px;">You can chat about the event here</h1>

<div class="container chat">

  <div id="messages" class="messages">
    <% @event.messages.each do |message| %>
      <div id="message-<%= message.id %>">
        <small>
          <strong><%= message.user.profile_name %></strong>
          <i><%= message.created_at.strftime("%a %b %e at %l:%M %p") %></i>
        </small>
        <p><%= message.content %></p>
      </div>
    <% end %>
  </div>
</div>

<%= simple_form_for [@event, @message],
  html: {class: "d-flex w-25 mx-5 justify-content-center"} do |f|
%>
  <% other_user = current_user == @event.user %>
  <%= f.input :content,
    label: false,
    # placeholder: "Message #{other_user.profile_name}",
    wrapper_html: {class: "flex-grow-1"}
  %>
  <%= f.submit "Send", class: "btn btn-primary mb-3" %>
<% end %>
